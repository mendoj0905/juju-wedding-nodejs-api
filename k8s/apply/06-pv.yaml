apiVersion: v1
kind: PersistentVolume
metadata:
  name: wedding-pv-volume
  namespace: juju-wedding
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"