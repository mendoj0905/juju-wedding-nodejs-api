import WebPass from './webPassDAL';
import PasswordUtil from '../../util/PasswordUtil';

export default class WebPassService {

  static async checkPassword(wedding, password) {
    try {
      
    } catch (e) {
      console.error(e);
    }
  }
}